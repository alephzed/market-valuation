// Bounded context: Identity (generic). Users, Watchlists, Alert
// configuration, and RBAC tiers. Kept deliberately thin.
plugins {
    id 'java-library'
}

dependencies {
    implementation platform(libs.spring.boot.dependencies)

    api project(':domain-core')
    implementation 'org.springframework:spring-context'
}
